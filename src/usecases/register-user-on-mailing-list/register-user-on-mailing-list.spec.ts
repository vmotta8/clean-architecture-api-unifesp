import { UserData } from '../../entities/user-data'

describe('Register user on mailing list', () => {
  it('should add user with complete data to mailing list', async () => {
    const users: UserData[] = []
    // const repo: UserRepository = new InMemoryUserRepository(users)
    // const usecase: RegisterOnMailingList = new RegisterOnMailingList(repo)

    // const name = 'Vinicius'
    // const email = 'vinicius@email.com'

    // const response = await usecase.RegisterOnMailingList({ name, email })

    // const user = repo.FindUserByEmail(email)
    // expect((await user).name).toBe(name)
    expect(users).toBe(users)
  })
})
